# HW3: JIT Compilation and Tracing for ML on GPUs

## Requirements
pip install jax jaxlib torch torchvision matplotlib functorch

## Running
1. Open each notebook in Google Colab
2. Set runtime to GPU: Runtime → Change runtime type → T4 GPU
3. Run all cells in order
